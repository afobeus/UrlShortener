# URL Shortener Service (C++ Backend + Flask Frontend)

Проект представляет собой полнофункциональный сервис сокращения ссылок, состоящий из:

* **Backend на C++**
  Реализован на базе Boost.Asio + Boost.Beast (HTTP-сервер) и SQLite (хранение данных).
  Отвечает за генерацию коротких ссылок и получение исходных URL.

* **Frontend на Python (Flask)**
  Небольшой веб-интерфейс, работающий с backend через HTTP-запросы.

---

## 📌 Возможности

### Backend (C++)

* Принимает запросы по протоколу HTTP.
* Генерирует короткие коды из 7 символов (буквы и цифры).
* Хранит соответствия `short_code ↔ original_url` в SQLite.
* Возвращает уже существующее сокращение, если URL был сокращён ранее.
* Декодирует короткий URL и возвращает исходный.
* Эндпоинты:

  * `GET /makeshort/<url>` — создание короткой ссылки
  * `GET /<code>` — получение оригинального URL по короткому коду
  * `GET /` и `/health` — статус сервиса

### Frontend (Flask)

* Простая форма для ввода URL.
* Отправляет запросы на C++ сервер.
* Отображает результат пользователю.
* Поддерживает редиректы по коротким URL.

---

## 🛠️ Технологии

### Backend

* **C++17**
* **Boost.Asio / Boost.Beast** — HTTP сервер
* **SQLiteCpp** — работа с SQLite
* `std::regex`, `std::random_device`, `std::mt19937`

### Frontend

* **Python 3**
* **Flask**
* **Requests**

---

## 🚀 Установка и запуск

### 1. Backend (C++)

**Зависимости:**

* Boost
* SQLite3
* SQLiteCpp
* Компилятор с поддержкой C++17

**Сборка (пример через g++):**

```bash
g++ main.cpp -o url_shortener \
    -lboost_system -lboost_thread -lboost_beast \
    -lsqlite3 -lSQLiteCpp -pthread
```

**Запуск:**

```bash
./url_shortener
```

Сервер запустится на порту **8080**.

---

### 2. Frontend (Flask)

**Установка зависимостей:**

```bash
pip install flask requests
```

**Запуск фронтенда:**

```bash
python app.py
```

Фронтенд будет доступен по адресу:
➡ **[http://localhost:5000](http://localhost:5000)**

Важно: фронтенд делает запросы на `http://localhost:8080`, поэтому бэкенд должен быть запущен.

---

## 🔗 Примеры запросов к Backend

### Создать короткую ссылку:

```
GET http://localhost:8080/makeshort/https://google.com
```

Ответ:

```
afobeus.ru/Ab3F9Ks
```

### Получить исходный URL по короткому коду:

```
GET http://localhost:8080/Ab3F9Ks
```

Ответ:

```
https://google.com
```

---

## 📁 Структура проекта

```
project/
│
├── backend/
│   └── main.cpp
│
├── frontend/
│   ├── main.py
│   ├── templates/
│   │   ├── index.html
│   │   └── result.html
│   └── static/
│
└── README.md
```

---

## 🗄️ База данных

При первом запуске backend создаст файл:

```
urls.db
```

Таблица `urls`:

| id | short_code | original_url | created_at |
| -- | ---------- | ------------ | ---------- |

Также создаются индексы для ускорения поиска.
